# bcon — AI-mediated Container Operations System

**RFC + Product Specification**

| 항목 | 내용 |
|-----|------|
| Status | Draft v1.0 |
| Author | Pablo |
| Created | 2024-12 |
| Last Updated | 2024-12 |

---

## 0. Executive Summary

컨테이너 운영의 핵심 문제는 **도구 부족이 아니라 판단 부담**이다.

- 컨테이너는 쉽게 띄울 수 있다
- 문제를 이해하고 판단하는 것은 여전히 사람 몫이다
- Kubernetes는 이 판단을 자동화하려 했지만, 그 대가로 과도한 복잡성과 학습 비용을 요구한다

**bcon**은 이 문제를 다른 방향에서 해결한다.

> **bcon은 "실행"이 아니라 "판단"을 AI에게 위임하는 단계적 컨테이너 운영 시스템이다.**

이 RFC는 bcon을 **3단계(Level 1–3)**로 나누어 정의한다.

---

## 1. Core Idea

### 1.1 컨테이너 운영의 본질

모든 컨테이너 운영 시스템은 다음 질문에 답하려 한다:

```
1. 지금 상태는 정상인가?
2. 아니라면, 왜 그런가?
3. 그럼 무엇을 해야 하는가?
```

- **Kubernetes**는 이 질문을 `controller loop`로 자동화했다
- **bcon**은 이 질문을 `AI-mediated reasoning loop`로 해결한다

### 1.2 Fundamental Principle

> **Judgment before Action**

- Action 없이도 가치는 있다
- Judgment 없는 Action은 위험하다

bcon은 이 원칙을 **아키텍처 레벨에서 강제**한다.

### 1.3 왜 Kubernetes가 아닌가

| 구성 요소 | 학습 곡선 | 실제 필요도 (소규모) |
|----------|----------|-------------------|
| YAML 매니페스트 문법 | 높음 | 낮음 |
| 네트워킹 (Service, Ingress, CNI) | 매우 높음 | 중간 |
| 스토리지 (PV, PVC, StorageClass) | 높음 | 낮음 |
| RBAC, ServiceAccount | 높음 | 낮음 |
| Helm, Kustomize | 중간 | 낮음 |
| 모니터링 (Prometheus, Grafana) | 높음 | 중간 |

**현실:**
- 중소기업/스타트업의 80%는 컨테이너 10개 미만 운영
- 개인 개발자 대부분은 단일 서버에서 2-5개 컨테이너 운영
- 이들에게 etcd 클러스터, control plane HA, CNI 플러그인 선택은 불필요한 오버헤드

---

## 2. System Levels

bcon은 하나의 제품이 아니라 **진화 가능한 시스템**이다.

```
Level 1 ── Observe & Explain    (Read-only, OSS)
Level 2 ── Decide with Human    (Guided Ops)
Level 3 ── Execute with Guardrails (AI-mediated Ops)
```

각 레벨은 **완전한 제품**이며, 이전 레벨을 **절대 깨지 않는다**.

### Level Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        System Layers                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   Level 3: Execution Layer (AI-mediated Ops)                    │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  • 자율 실행 (Decision Guardrails)                       │   │
│   │  • 다중 호스트 관리                                       │   │
│   │  • 배포 전략 (롤링, 카나리)                               │   │
│   └─────────────────────────────────────────────────────────┘   │
│                              ▲                                   │
│   Level 2: Decision Layer (Guided Ops)                          │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  • 해결책 제안 + 승인 요청                                │   │
│   │  • Intent-to-YAML 변환                                   │   │
│   │  • 승인 기반 실행                                        │   │
│   └─────────────────────────────────────────────────────────┘   │
│                              ▲                                   │
│   Level 1: Sensor Layer (Read-only)                             │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  • 상태 관찰 및 수집                                      │   │
│   │  • 문제 진단 및 설명                                      │   │
│   │  • Drift Detection                                       │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. Level 1 — Observe & Explain

> **"지금 무슨 일이 벌어지고 있는지 인간이 이해할 수 있는 언어로 설명한다."**

### 3.1 목적

Level 1은 **Sensor + Reasoning Layer**만 담당한다.

```
Signals → Symptoms → Likely Causes → Evidence → Suggestions
```

### 3.2 Scope (강제 제한)

| 허용 (Read-only) | 금지 |
|-----------------|-----|
| 컨테이너 상태 조회 | 컨테이너 생성/삭제 |
| 리소스 사용량 조회 | 재시작 |
| 로그 조회 및 분석 | 배포 |
| Drift Detection | 스케일링 |
| 트러블슈팅 (진단) | 설정 변경 |
| Cost 추적 | 어떤 쓰기 작업도 금지 |

> ⚠️ **이 제한은 기술적 제약이 아니라 철학적 계약이다.**

### 3.3 Core Features

#### 3.3.1 트러블슈팅 (킬러 피처)

```
사용자: "api-server 응답이 느려"

bcon: [자동 분석]
      ├─ 컨테이너 리소스 사용량 조회
      ├─ 최근 로그 분석 (에러 패턴)
      ├─ 의존 서비스 상태 체크
      └─ 최근 변경 이력 확인

      "분석 결과:
      
      🔍 증상
      - api-server 응답시간: 2.3s (평소 45ms)
      
      🎯 원인 추정 (신뢰도 순)
      1. [95%] postgres 쿼리 지연
         - 'orders' 테이블 full scan 감지
      
      💡 권장 조치
      - postgres 리소스 증설 고려
      - orders.created_at 인덱스 검토"
```

**왜 킬러 피처인가:**
- Coolify/Dokku: 배포 ✅, 장애 분석 ❌
- bcon: 배포 ❌, **장애 분석 ✅**

#### 3.3.2 Drift Detection (상태 드리프트 탐지)

Git(Desired State)과 Docker(Actual State) 간의 불일치를 자동 탐지한다.

```
┌─────────────────┐     ┌─────────────────┐
│   Git Repo      │     │  Docker Engine  │
│  (Desired)      │     │   (Actual)      │
│                 │     │                 │
│ services:       │     │ $ docker ps     │
│   api: 3개      │ ──▶ │ api: 2개 실행중  │
│   redis: 1개    │     │ redis: 0개 ❌   │
└─────────────────┘     └─────────────────┘
         │                      │
         └──────────┬───────────┘
                    ▼
    "⚠️ 드리프트 감지:
     - api: 3개 예상, 2개 실행 중
     - redis: 실행되어야 하나 중지됨"
```

**체크 주기:** 5분 (설정 가능)

**리포트 형식:**
```yaml
drift_report:
  timestamp: 2024-12-22T10:30:00Z
  status: DRIFTED
  
  differences:
    - service: api
      expected: { replicas: 3 }
      actual: { replicas: 2 }
      
    - service: redis
      expected: { status: running }
      actual: { status: exited }
      
  untracked:
    - container: "test-nginx-temp"
      recommendation: "제거 또는 Git에 추가"
```

#### 3.3.3 Cost Tracking (비용 추적)

API 호출 비용을 추적하고 리포팅한다. (제한은 Level 2+)

```
📊 비용 현황
- 오늘: $0.42
- 이번 주: $2.10
- 이번 달: $8.50

작업 유형별:
  트러블슈팅  $3.20 (45%)
  상태 분석   $1.80 (25%)
```

### 3.4 Architecture (Level 1)

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │     CLI      │  │   Chat UI    │  │  Slack Bot   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      bcon Core (Level 1)                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                 Signal Collectors                        │    │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐              │    │
│  │  │  Docker   │ │   Logs    │ │  Compose  │              │    │
│  │  │  Stats    │ │  Reader   │ │  Parser   │              │    │
│  │  └───────────┘ └───────────┘ └───────────┘              │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│                              ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                  Analysis Engine                         │    │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐              │    │
│  │  │  Drift    │ │ Trouble-  │ │   Cost    │              │    │
│  │  │ Detector  │ │  shooter  │ │  Tracker  │              │    │
│  │  └───────────┘ └───────────┘ └───────────┘              │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│                              ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │               Explanation Renderer                       │    │
│  │           (Human-readable output)                        │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Infrastructure (Read-only)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Docker API   │  │  Log Files   │  │  Git Repo    │          │
│  │ (inspect,    │  │              │  │  (Desired    │          │
│  │  stats, logs)│  │              │  │   State)     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.5 Value Proposition

| 가치 | 설명 |
|-----|-----|
| 안전성 | 프로덕션에서 두려움 없이 실행 가능 |
| 즉각적 가치 | "왜 느린지" 바로 이해 |
| 낮은 진입 장벽 | Read-only = 신뢰 불필요 |
| 기반 레이어 | Level 2, 3의 토대 |

### 3.6 Success Criteria (Level 1)

| 메트릭 | 목표 |
|-------|-----|
| "왜 느린지 이해했다" | 사용자 피드백 80%+ 긍정 |
| "이건 위험하지 않다" | 프로덕션 도입 거부율 < 10% |
| 장애 원인 정확도 | 80%+ |

---

## 4. Level 2 — Decide with Human

> **"AI가 판단을 내리고, 사람은 선택만 한다."**

### 4.1 목적

Level 1 위에 **Decision Layer**가 추가된다.

```
Explanation (Level 1)
        ↓
Proposed Actions (ranked, risk-scored)
        ↓
Human Approval
        ↓
Execution
```

### 4.2 추가 기능

#### 4.2.1 해결책 제안 + 승인 요청

```
bcon: "postgres 메모리 압박 감지됨.

      옵션:
      1. 컨테이너 재시작 (위험도: 낮음)
      2. 메모리 제한 2GB로 증가 (위험도: 중간)
      3. 마지막 배포 롤백 (위험도: 중-상)

      어떤 조치를 취할까요?"

사용자: "1번"

bcon: [재시작 실행]
      "완료. 현재 메모리 사용량: 45%"
```

#### 4.2.2 Intent-to-YAML (하이브리드 상태 관리)

자연어 의도를 Docker Compose YAML로 변환한다.

```
사용자: "api 서버를 3개로 늘리고 메모리 1GB로 설정해줘"

bcon: "compose.yaml 수정안:

      api:
        deploy:
          replicas: 3      # 변경: 1 → 3
          resources:
            limits:
              memory: 1G   # 변경: 512M → 1G

      적용할까요?"

사용자: "응"

bcon: [Git commit + docker compose up]
```

**왜 YAML인가:**
- 순수 자연어 → 감사(Audit)/재현성 부족
- YAML → GitOps 호환, AI 불가용 시 수동 복구 가능

#### 4.2.3 Cost Budgeting (비용 제한)

Level 1의 추적에 **제한 기능** 추가.

```
┌─────────────────────────────────────────────────────────────┐
│                    Cost Budgeting                            │
├─────────────────────────────────────────────────────────────┤
│  📊 예산 현황                                                │
│  오늘: $0.42 / $5.00 (일일 한도)                             │
│  ████████░░░░░░░░░░░░ 8%                                    │
│                                                             │
│  🚨 임계값                                                   │
│  ├─ 50%: 알림                                               │
│  ├─ 80%: 비필수 작업 제한                                    │
│  └─ 100%: 긴급 작업만 허용                                   │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 Architecture (Level 2 추가분)

```
Level 1 (전체)
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│                    Decision Engine                           │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐    │
│  │    Action     │  │     Risk      │  │    Intent     │    │
│  │   Proposer    │  │    Scorer     │  │   to YAML     │    │
│  └───────────────┘  └───────────────┘  └───────────────┘    │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│                   Approval Workflow                          │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐    │
│  │    Human      │  │    Audit      │  │     Cost      │    │
│  │   Approval    │  │     Log       │  │    Budget     │    │
│  └───────────────┘  └───────────────┘  └───────────────┘    │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│                  Execution Adapter                           │
│  ┌───────────────┐  ┌───────────────┐                       │
│  │  Docker API   │  │   SSH (원격)   │                       │
│  │  (write ops)  │  │               │                       │
│  └───────────────┘  └───────────────┘                       │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 Success Criteria (Level 2)

| 메트릭 | 목표 |
|-------|-----|
| "결정이 쉬워졌다" | 의사결정 시간 50% 단축 |
| "실수할 확률이 줄었다" | 롤백 빈도 30% 감소 |
| MTTR 개선 | 평균 복구 시간 50% 단축 |

---

## 5. Level 3 — Execute with Guardrails

> **"사람의 운영 의도를 AI가 안전하게 실행한다."**

### 5.1 목적

**AI-mediated reconciliation** — Kubernetes의 컨트롤러 루프를 AI가 대체.

```
Desired State (Git)
        +
Actual State (Docker)
        +
Policy (Decision Guardrails)
        +
Risk Budget
        ↓
AI가 판단 후 실행
```

### 5.2 Decision Guardrails

모든 action은 위험도에 따라 분류된다:

```
┌─────────────────────────────────────────────────────────────────┐
│                    Decision Guardrails                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🟢 자율 실행 (Autonomous)                                      │
│     ├─ 상태 조회, 로그 조회, 메트릭 조회                          │
│     ├─ 리소스 스케일 업 (사전 정의된 한도 내)                      │
│     ├─ 비핵심 컨테이너 재시작                                     │
│     └─ 헬스체크 기반 자동 복구 (동일 설정)                        │
│                                                                 │
│  🟡 알림 후 실행 (Notify & Execute)                              │
│     ├─ 새 버전 배포 (스테이징)                                    │
│     ├─ 설정 변경 (환경변수, 포트)                                 │
│     └─ 스케일 다운 (최소 1개 유지)                                │
│                                                                 │
│  🔴 명시적 승인 필요 (Explicit Approval)                         │
│     ├─ 프로덕션 배포                                             │
│     ├─ 볼륨/데이터 삭제                                          │
│     ├─ 네트워크 토폴로지 변경                                     │
│     └─ 시크릿/인증 정보 변경                                      │
│                                                                 │
│  ⛔ 금지 (Prohibited - MFA + 2인 승인)                           │
│     ├─ 전체 환경 삭제                                            │
│     ├─ 백업 삭제                                                 │
│     └─ 보안 정책 비활성화                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 Multi-Host Management

Git 중심 컨트롤러 + SSH 기반 제어.

```
┌─────────────────────────────────────────────────────────────┐
│                   Control Plane (로컬/중앙)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │    bcon     │  │  Git Repo   │  │   ssh-mcp   │          │
│  │  (AI Core)  │  │  (State)    │  │  (Executor) │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
    ┌──────────┐     ┌──────────┐     ┌──────────┐
    │ Host A   │     │ Host B   │     │ Host C   │
    │ Docker   │     │ Docker   │     │ Docker   │
    │(No Agent)│     │(No Agent)│     │(No Agent)│
    └──────────┘     └──────────┘     └──────────┘
```

**핵심 원칙:**
- **No Agent on Nodes**: 각 노드에 에이전트 없음 (K8s 회귀 방지)
- **SSH as Universal Interface**: 모든 제어는 SSH를 통해
- **Git as State Store**: Desired State는 Git에 영구 저장
- **Overlay Network**: Wireguard/Tailscale로 보안 통신

### 5.4 Advanced Features

#### 배포 전략
- 롤링 업데이트
- 카나리 배포
- 블루-그린 배포

#### 자동 복구
```python
class RecoveryPolicy:
    max_failure_rate = 0.1  # 10% 이상 실패 시 중단
    health_check_timeout = 60
    auto_rollback = True
    keep_previous = 1
```

### 5.5 Success Criteria (Level 3)

| 메트릭 | 목표 |
|-------|-----|
| "운영 피로도가 사라졌다" | 수동 개입 80% 감소 |
| "AI를 신뢰한다" | 자율 실행 허용률 70%+ |
| 비용 절감 | 전통 DevOps 대비 90%+ |

---

## 6. Target Users

### Persona 1: 솔로 개발자

```
이름: 민수
환경: 단일 VPS (4GB RAM)
운영: Next.js + PostgreSQL + Redis
Pain Point: 배포/장애 시 수동 작업
적합 Level: Level 1 → Level 2
```

### Persona 2: 스타트업 백엔드 팀

```
이름: 영희
환경: AWS EC2 3대
운영: 마이크로서비스 5개
Pain Point: K8s 학습/운영 비용
적합 Level: Level 2 → Level 3
```

### Persona 3: DevOps 없는 회사

```
이름: 철수 (데이터 엔지니어)
환경: 온프레미스 2대
운영: Airflow + 데이터 파이프라인
Pain Point: 인프라에 50% 시간 소요
적합 Level: Level 1 → Level 2
```

---

## 7. Security Architecture

### 7.1 Level별 보안 모델

| Level | 권한 | 위험도 |
|-------|-----|-------|
| Level 1 | Read-only | 최소 |
| Level 2 | Write (승인 필요) | 중간 |
| Level 3 | Write (자율 가능) | 관리 필요 |

### 7.2 프롬프트 인젝션 방어 (Level 2+)

```
1. 입력 검증
   ┌─────────────────────────────────────┐
   │ 허용된 명령어 Allowlist 검증         │
   │ docker run, stop, logs ... ✅      │
   │ rm -rf /, curl | bash ... ❌       │
   └─────────────────────────────────────┘

2. 출력 필터링
   ┌─────────────────────────────────────┐
   │ 실행 전 명령어 샌드박스 검증          │
   │ 위험 패턴 탐지 및 차단               │
   └─────────────────────────────────────┘

3. 컨텍스트 격리
   ┌─────────────────────────────────────┐
   │ 사용자 입력 ≠ 시스템 지시            │
   │ 권한 스코프 분리                     │
   └─────────────────────────────────────┘
```

### 7.3 Audit Log (필수)

```yaml
audit_log:
  timestamp: 2024-12-22T10:30:00Z
  actor: user/pablo
  action: container.restart
  target: api-server
  approval: explicit
  result: success
  context:
    reason: "memory pressure detected"
    proposed_by: bcon
```

---

## 8. Technical Stack

### 8.1 Container Runtime

| 옵션 | 권장 | 이유 |
|-----|-----|-----|
| Docker | ✅ Level 1-2 | 보편적, 안정적 |
| Podman | Level 3+ | 데몬리스 옵션 |

### 8.2 State Storage

| 용도 | 권장 |
|-----|-----|
| Desired State | Git (Docker Compose) |
| Audit/History | SQLite |
| Runtime State | Docker API 직접 조회 |

### 8.3 Networking (Level 3)

| 옵션 | 용도 |
|-----|-----|
| Docker Network | 기본 통신 |
| Wireguard/Tailscale | 다중 호스트 오버레이 |
| Nginx/Traefik | 리버스 프록시 |

---

## 9. Roadmap

### Phase 0: Level 1 MVP (bcon v0.1)

**목표:** Read-only 진단 도구

| 기능 | 우선순위 |
|-----|---------|
| 상태 조회 | 🔴 핵심 |
| 트러블슈팅 (진단) | 🔴 핵심 |
| Drift Detection | 🔴 핵심 |
| 로그 분석 | 🟡 중요 |
| Cost 추적 | 🟡 중요 |

**기간:** 2-4주

### Phase 1: Level 2 MVP

**목표:** 승인 기반 실행

| 추가 기능 | 우선순위 |
|---------|---------|
| 해결책 제안 | 🔴 핵심 |
| Intent-to-YAML | 🔴 핵심 |
| 승인 워크플로우 | 🔴 핵심 |
| Cost Budgeting | 🟡 중요 |
| 원격 SSH 실행 | 🟡 중요 |

**기간:** 4-8주

### Phase 2: Level 3

**목표:** AI-mediated Operations

| 추가 기능 |
|---------|
| Decision Guardrails |
| 다중 호스트 관리 |
| 배포 전략 (롤링, 카나리) |
| 자동 복구 |

**기간:** 8-16주

---

## 10. Non-Goals

❌ Kubernetes 기능 parity
❌ 고성능 실시간 제어 (ms 단위)
❌ 대규모 멀티테넌시
❌ Level 1에서 어떤 쓰기 작업도

> bcon은 소형~중형 운영자의 **판단 보조**에 집중한다.

---

## 11. Why This Approach

### 11.1 왜 한 번에 Level 3로 안 가는가

```
신뢰는 행동 전에 쌓여야 한다.
OSS에서는 특히 더 그렇다.
Level 1이 없으면 Level 3은 공포다.
```

### 11.2 왜 Level 1이 standalone이어야 하는가

- "AI가 내 서버 만진다"는 거부감 제거
- 관찰 → 신뢰 → 위임의 자연스러운 흐름
- 실패해도 시스템에 영향 없음

### 11.3 bcon의 정체성

```
bcon은 Kubernetes의 대체재가 아니다.
bcon은 Kubernetes 이전의 공백을 채운다.

bcon answers "why" before answering "how".
```

---

## 12. Summary

| Level | 목적 | 핵심 가치 |
|-------|-----|---------|
| Level 1 | Observe & Explain | "이해했다" |
| Level 2 | Decide with Human | "결정이 쉬워졌다" |
| Level 3 | Execute with Guardrails | "운영이 사라졌다" |

**Judgment before Action.**

---

## Appendix

### A. 용어 정의

| 용어 | 정의 |
|-----|-----|
| Desired State | Git에 저장된 원하는 인프라 상태 |
| Actual State | 현재 Docker에서 실행 중인 상태 |
| Drift | Desired와 Actual 간의 차이 |
| Decision Guardrails | 작업 위험도별 승인 정책 |

### B. 변경 이력

| 날짜 | 버전 | 변경 내용 |
|-----|-----|---------|
| 2024-12 | 0.1~0.3 | 초기 RFC (단일 Phase 구조) |
| 2024-12 | 1.0 | Level 기반 구조로 전면 재구성 |
|         |     | "Judgment before Action" 원칙 명시 |
|         |     | Level 1 Read-only 제약 확립 |
